import * as React from 'react';
import { Box } from '@mui/material';

interface Props {}

const MobileBreadcrumbs: React.FC<Props> = ({}) => {
    return <Box sx={{ flex: 1 }}>BC</Box>;
};

export default MobileBreadcrumbs;
